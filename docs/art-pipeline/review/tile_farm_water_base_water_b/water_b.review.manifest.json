{
  "version": 1,
  "id": "review_tile_farm_water_base_water_b",
  "_reviewOnly": "This manifest is a review/evidence artifact only. It is NOT the production manifest for tile_farm_water_base and its output is NOT a runtime asset. Do not load target.outputPath in Phaser or reference it from a production manifest.",
  "_sourceNote": "water_b is a CANDIDATE runtime master derived deterministically from the approved water_a runtime pixels by 18 adjacent interior pair swaps (seed 33199). Borders and the 1px buffer are byte-identical to water_a; histogram and palette are preserved exactly (colours are only permuted). The exact 16x16 master is upscaled by 64x nearest-neighbour block replication to the canonical 1024x1024 source. This manifest proves the real pipeline returns the master with zero drift. Verdict pending ChatGPT audit.",
  "target": {
    "outputPath": "water_b.review-normalized.png",
    "cellPx": [16, 16],
    "cols": 1,
    "rows": 1
  },
  "sources": {
    "water_b_source": {
      "path": "../../../../assets/source/generated/tile_farm_water_base/water_b.png",
      "background": {
        "mode": "alpha"
      }
    }
  },
  "frames": [
    {
      "sourceRef": "water_b_source",
      "destCell": [0, 0],
      "trim": "none",
      "fit": "fill",
      "anchor": "top_left"
    }
  ]
}
