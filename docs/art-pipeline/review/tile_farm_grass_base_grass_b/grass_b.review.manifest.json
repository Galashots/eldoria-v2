{
  "version": 1,
  "id": "review_tile_farm_grass_base_grass_b",
  "_reviewOnly": "This manifest is a review/evidence artifact only. It is NOT the production manifest for tile_farm_grass_base (grass_c does not exist yet) and its output is NOT a runtime asset. Do not load target.outputPath in Phaser or reference it from a production manifest.",
  "_sourceNote": "grass_b is an APPROVED RUNTIME MASTER derived deterministically from the approved grass_a runtime pixels by 24 interior pair swaps. The exact 16x16 master is upscaled by 64x nearest-neighbour block replication to the canonical 1024x1024 source. This manifest proves the real pipeline returns the master with zero drift.",
  "target": {
    "outputPath": "grass_b.review-normalized.png",
    "cellPx": [16, 16],
    "cols": 1,
    "rows": 1
  },
  "sources": {
    "grass_b_source": {
      "path": "../../../../assets/source/generated/tile_farm_grass_base/grass_b.png",
      "background": {
        "mode": "alpha"
      }
    }
  },
  "frames": [
    {
      "sourceRef": "grass_b_source",
      "destCell": [0, 0],
      "trim": "none",
      "fit": "fill",
      "anchor": "top_left"
    }
  ]
}
