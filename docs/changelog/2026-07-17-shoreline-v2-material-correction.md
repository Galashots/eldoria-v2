# 2026-07-17 — Shoreline terrain-blend family v2 material correction — verdict pending

- Author/branch: ChatGPT (visual-QA lead, prescribed the exact v2 recipe) + Claude Code (repo agent), `chatgpt/shoreline-terrain-family` (draft PR #99). Response to ChatGPT's blocking visual + implementation review of the v1 shoreline family.
- Scope: ChatGPT's independent review found v1 read as a continuous bright mustard/gold outline with an inner-corner bullseye and near-invisible moss, and prescribed an exact corrective material-selection recipe (band geometry/topology/seed/palette unchanged): inner sand base flips `#D5A342→#B98535` (rare `hash%11==0` accent to `#D5A342`); outer sand base flips to `#926B2A` with interior `hash%4==0` feathering to exact `grass_a` and `hash%3==0` to `#6C8B15`; moss band gains an `hash%5==0` `#926B2A` interlock ahead of its existing light/dark-moss/grass chain. Implemented verbatim in `composeShorelineCell()`, verified per-pixel against ChatGPT's literal formulas by a dedicated test. Also fixed two verification gaps ChatGPT flagged: adjacency coverage is now exhaustive (100 directed pairs — 50 horizontal + 50 vertical — across all 14 usable corner codes, not a lattice sample) and the band-class/sand-RGB mismatch counters are now hard compositor failures (`assertNoSharedEdgeMismatches`, unit-tested directly) rather than only reported fields; `npm run test:terrain-blend` is now a named CI step in `.github/workflows/ci.yml`.
- Principal files: `scripts/compose-terrain-blend-family.mjs` (v2 material logic + hard-throw guard + exhaustive enumeration), `scripts/test-shoreline-terrain-blend.mjs` (16 groups, incl. 2 new: exhaustive adjacency, hard-throw unit test), `.github/workflows/ci.yml`, regenerated `docs/art-pipeline/review/tile_farm_water_shore_family/` (recipe, AUDIT with a new honest v2-correction section, family-report, evidence, plus new `ab-v1-vs-v2-comparison.png`), regenerated 12 `assets/source/generated/tile_farm_water_shore/*.png` canonical sources (`center.png` unchanged — still an exact copy of approved `water_a`), regenerated `assets/tilesets/tile_farm_water_shore.png`, `docs/CURRENT_STATE.md`, `docs/CHATGPT_CHANGELOG.md`.
- Verification facts: `297/342` (86.8%) of sand-classified pixels recoloured; mean luminance across sand-swatch pixels dropped `145.35→125.05` (14.0% darker); adjacency: 100 directed pairs (50h+50v), 0 band-class mismatches, 0 sand-to-sand RGB mismatches, both hard-enforced (`assertNoSharedEdgeMismatches` throws, directly unit-tested); packed-sheet SHA `281c2f639d501b7a868fdbf974c8ed4a14dc31f019d3f305b80f8724091daaff` stable across two clean recompositions; dirt non-regression re-proven (packed sheet + all 12 canonical sources byte-identical to `main`, verified inside the test suite against the real committed files). Local green: `npm ci`, `test:terrain-blend` (dirt 14 groups + shoreline 16 groups), `test:asset-pipeline`, `test:asset-review`, `check`, `normalize:asset`, `validate:asset`.
- Verdict: **not yet recorded — ChatGPT's second review is pending.** Disclosed honestly in `AUDIT.md` §0: v2 measurably mutes the sand band (14.0% mean luminance drop, 86.8% of sand pixels recoloured) but does not eliminate the continuous-ring silhouette at `8×`, because it only reassigns swatches within existing q-bands per ChatGPT's explicit instruction to preserve band geometry/topology. The previous audit's "no hard outline … or halo" claim is withdrawn; see `ab-v1-vs-v2-comparison.png` for the actual rendered pixels. Whether v2 clears the visual bar or needs a v3 (e.g. band-width variation, more aggressive inner-sand feathering) is ChatGPT's call, not self-certified here.
- Compatibility: source/review/packed-sheet only. `runtimeIntegrated:false`, `mapIntegrated:false`, `physicalIpadValidated:false`, `childValidated:false`. No Phaser preload, Wangset/Tiled wiring, `public/maps/farm.json`, collision, runtime shimmer/animation, save, gameplay, quest, curriculum, mastery, reward, dependency, or migration change. No shoreline decals, reeds, stones, foam, or lily pads.
- Remaining risk: visual acceptance is unresolved pending ChatGPT's second verdict; a v3 may be needed if the ring read is still judged too strong after review. Nothing is runtime-integrated, so no gameplay or iPad impact regardless of outcome.
