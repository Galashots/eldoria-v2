# 2026-07-17 — Deterministic terrain-blend compositor + dirt family (13 cells)

- Author/branch: ChatGPT (spec/art-direction + visual-QA lead) + Claude Code (repo agent), `chatgpt/dirt-terrain-compositor` (draft PR #98). Level-2 bounded implementation of `docs/art-pipeline/TERRAIN_BLEND_COMPOSITOR_SPEC_V1.md`.
- Scope: added a generic, manifest-driven compositor (`scripts/compose-terrain-blend-family.mjs`) with exported pure core functions and focused tests (`scripts/test-terrain-blend-compositor.mjs`, 14 groups). Completed `tile_farm_path_dirt` as a 13-cell source/review/packed family by compositing the approved `tile_farm_path_dirt / center` dirt over approved `tile_farm_grass_base / grass_a`, using a four-corner integer-bilinear mask (corner order `[nw,ne,se,sw]`, `q = 2·score − 225`) plus a one-pixel deterministic material interlock (fringe-eligible `x,y∈[1,14]` and `|q|≤30`; locked hash seed `0x0000D17A`; invert when `hash%3===0`). No image generation: every output pixel is copied verbatim from one of the two approved inputs at the same coordinate.
- Principal files: the two scripts; `docs/art-pipeline/review/tile_farm_path_dirt_family/` (recipe, AUDIT, family-report, `masks-13x1.png`, `family-contact-sheet.png`, `mixed-tiling-preview.png`); 12 new `assets/source/generated/tile_farm_path_dirt/*.png` canonical sources (`center.png` unchanged); `assets/manifests/tile_farm_path_dirt.manifest.json`; `assets/tilesets/tile_farm_path_dirt.png` (`208×16`); package scripts `compose:terrain-blend` and `test:terrain-blend`; `docs/CURRENT_STATE.md`; `docs/CHATGPT_CHANGELOG.md`.
- Verification facts: exact core-foreground occupancy (center 256, edges 128, corners 41, inner corners 215); per tile alpha `256 opaque / 0 partial / 0 transparent`, `0` unexpected colours, `256/256` within RGB distance 40 of `forest + wood_leather` swatches (max nearest-swatch distance ≈ 32.5); `center` byte-identical to the approved dirt input (0 fringe-eligible pixels); canonical-source `64×` block exactness; `0` decoded-RGBA drift after real-pipeline normalization and after extraction from the packed sheet; mixed `7×7` field with all required topologies present and `0` incompatible shared edges; `84` shared-edge pairs with `0` material-class mismatches; packed-sheet SHA `b9da641b…dddf7a` stable across two clean recompositions. Local green: `test:terrain-blend`, `test:asset-pipeline`, `test:asset-review`, `check`, `normalize:asset`, `validate:asset`.
- Verdict: final formal visual verdict is ChatGPT's, from the exact runtime pixels and the family evidence (contact sheet, mask sheet, mixed-tiling preview). This branch stops at a fully evidenced draft PR.
- Compatibility: source/review/packed-sheet only. `runtimeIntegrated:false`, `mapIntegrated:false`, `physicalIpadValidated:false`, `childValidated:false`. No Phaser preload, Wangset/Tiled wiring, `public/maps/farm.json`, collision, save, gameplay, quest, curriculum, mastery, profile, reward, dependency, or migration change. The shoreline family is deliberately not in this PR; the generic corner-code interface (water-as-foreground) is preserved for it.
- Remaining risk: dirt quietness and interlock noise-vs-straightness at `1×` are visual judgements reserved for ChatGPT. Shoreline sand/moss banding is unimplemented and may need a locked-swatch feasibility check (spec §10) before that family. Nothing is runtime-integrated, so there is no gameplay or iPad impact.
