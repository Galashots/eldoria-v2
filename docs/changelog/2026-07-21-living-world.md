# 2026-07-21 — Living World (M3)

- Author/branch: Codex (lead engineering agent), `world/living-world-m3`, draft PR #108. Build Prompt 03 executed on the verified M2 base; Phase 2 deliberately preceded Phase 1 because the registry definitions reference the village and Baker Pell introduced by Phase 2.
- Phase checkpoints: Phase 2 `56e19b36af0d2294a62c71549fc89a6363cd0137`; Phase 1 `79d91d1306443a71d3f84e04ab3e71589cd051cf`; Phase 3 `f956b6b81efeb1953f076487490ee86f3582c54e`; Phase 4 is the commit containing this document (exact SHA recorded in PR #108).
- Phase 1 — quest foundations: optional `questSteps` with save version still 2, a typed quest registry, the Berry Order definition, read-only Mira descriptors, and a generic `QuestSystem` that owns only `engine: 'registry'` quests.
- Phase 2 — village: added Eldoria Village, reciprocal farm gate, a continuous farm road, village collision/flavor interactions, Tiled validation, and browser coverage. No shop, gold sink, new art, or new audio was added.
- Phase 3 — dialogue and Berry Order: added reusable listener-safe dialogue presentation, shared single-utterance speech support, automatic Mage/manual Ranger read-aloud, Pell's offer/reminder/return/completion states, no-prompt sunberry gathering, persistent quest flags, and the fixed one-time 20-gold/Berry-Pie reward. Mira's flags and legacy state machine remain authoritative and unchanged.
- Phase 4 — cross-map compass: added validated `nextHop` routing derived from the real directed exit graph, pure objective-guidance resolution, exit-centre chevrons, edge-arrow routing, local-target restoration after travel, and Mira's `Head back to The Farm —` correction. Routing resolves only when objectives refresh; per-frame arrow work remains allocation-free position math.
- Review hardening: independent Phase 4 review confirmed the BFS graph, pure resolver, scaled exit centres, refresh-only routing, restart cleanup, and no new per-frame allocation. Two legacy browser expectations were corrected to the intended M3 behavior: active Mira objectives in the village route to GateToFarm, and completing Mira's chain promotes the Berry Order invitation and routes it to GateToVillage.
- Verification: `npm run typecheck` exit 0; `npm run test:unit` exit 0 (13 files, 148 tests); `npx playwright test --list` exit 0 (73 tests in 14 files); `npm run check` exit 0 (11 visual-target files / 37 targets, 43 transformed production modules, PWA/generated-surface/village-art validation); full `npx playwright test` exit 0 (73/73 in 5.0 minutes). Phase 3 exact-head GitHub Actions run [`29710792994`](https://github.com/Galashots/Eldoria-v2/actions/runs/29710792994) passed at `f956b6b81efeb1953f076487490ee86f3582c54e`; PR #108 records the final Phase 4 exact-head CI gate after push.
- Browser evidence: six named screenshots in [`docs/playtests/2026-07-21-living-world/`](../playtests/2026-07-21-living-world/README.md), including both player profiles, exact quest reward, village exit guidance, and corrected woods-to-farm guidance.
- Compatibility: `CURRENT_SAVE_VERSION` remains 2. `questSteps` and new flags are optional with graceful defaults; existing Mira flags win during save merging. No migration, profile-ID, curriculum, mastery, dependency, asset-pipeline, economy, or learning-gate change. Wrong answers and skips remain irrelevant to movement, travel, quest progress, baseline rewards, and story progression.
- Remaining risk: browser Chromium evidence is not physical-iPad Safari or child validation. Village/character presentation still uses approved bridge/placeholder-era art. Mossheart Ruins, Elder Rowan, shops, gold sinks, new art/audio, and the planned Zone #4 exit remain out of scope and unbuilt.
