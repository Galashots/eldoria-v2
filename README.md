# PR #133 evidence — Farm<->Village held-movement fix

Generating head: `ff1c735f58ae346180a860b5f13f50dbe8286999` (branch `claude/farm-village-held-movement-fix`).

Screenshots captured directly by `tests/farm-village-held-movement.spec.ts` on that exact head — real held keyboard input, no teleport. Each shows the arrival state immediately after the transition fires (banner/objective text visible), for both profiles and both directions.

- `ranger-farm-to-village.png` / `mage-farm-to-village.png` — walking west along the Farm road into Eldoria Village.
- `ranger-village-to-farm.png` / `mage-village-to-farm.png` — walking east along the Village road into the Farm.

This branch is evidence only and is never merged to `main`.
