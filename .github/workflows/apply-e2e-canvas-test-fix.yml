name: Apply narrow E2E Canvas test fix

on:
  pull_request:
    paths:
      - '.github/patch-e2e-canvas-test.py'
      - '.github/workflows/apply-e2e-canvas-test-fix.yml'
      - 'tests/vertical-slice.spec.ts'

permissions:
  contents: write

jobs:
  apply-fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Check out target branch
        run: |
          git fetch origin chatgpt/e2e-canvas-renderer
          git checkout -B target origin/chatgpt/e2e-canvas-renderer
      - name: Apply idempotent narrow correction
        run: python .github/patch-e2e-canvas-test.py
      - name: Commit correction when needed
        run: |
          if git diff --quiet -- tests/vertical-slice.spec.ts; then
            echo "No correction needed."
            exit 0
          fi
          git config user.name "ChatGPT Repo Agent"
          git config user.email "actions@users.noreply.github.com"
          git add tests/vertical-slice.spec.ts
          git diff --cached --check
          git commit -m "Allow one-tile E2E renderer movement variance"
          git push origin HEAD:refs/heads/chatgpt/e2e-canvas-renderer
